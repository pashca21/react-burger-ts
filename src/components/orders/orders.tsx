import React, { ReactNode } from 'react';
import styles from '@pages/profile/profile.module.css';

export const Orders = (): ReactNode => {
	return <div className={`${styles.form}`}>История заказов</div>;
};
